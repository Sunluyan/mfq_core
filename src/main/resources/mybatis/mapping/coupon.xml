<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mfq.dao.CouponMapper">
	<sql id="selectsql">
		id, uid, batch_id as batchId, coupon_num as couponNum, money, status, updated_at as updatedAt
	</sql>
	<sql id="insertsql">
		uid, batch_id, coupon_num, money, status, updated_at
	</sql>

	<select id="findByCouponNum" resultType="Coupon">
		select
		<include refid="selectsql" />
		from coupon where coupon_num=#{couponNum}
	</select>

	<select id="findByUid" resultType="Coupon">
		select
		<include refid="selectsql" />
		from coupon where uid=#{uid} and status =  order by updated_at desc
	</select>



	<select id="findUserValid" resultType="Coupon">
		select
		<include refid="selectsql" />
		from coupon where uid=#{uid} and status=#{status} and batch_id in
		<foreach collection="list" open="(" separator="," close=")" item="id">
            #{id}
        </foreach>
		order by updated_at desc
	</select>
	
	
	
	<select id="findCouponsByUidAndStatus" resultType="Coupon">
		select
		<include refid="selectsql" />
		from coupon where uid=#{uid} and status=#{status} 
		order by updated_at desc
	</select>
	
	<update id="updateStatus">
		update coupon set status=#{status} where coupon_num=#{couponNum}
	</update>

	<insert id="insertOne" useGeneratedKeys="true" keyProperty="id">
		<selectKey resultType="long" order="AFTER" keyProperty="id">
			SELECT LAST_INSERT_ID() AS id
		</selectKey>
		insert ignore into coupon(
			<include refid="insertsql" />
		) values (#{uid}, #{batchId}, #{couponNum}, #{money}, #{status}, #{updatedAt})
	</insert>

</mapper>